/** *********************************************************************************
 *
 * botbuilder-storage-redis
 *
 ********************************************************************************** */
import { RedisClient } from 'redis';
import { Storage, StoreItems } from 'botbuilder';
export declare class RedisDbStorage implements Storage {
    private redis;
    private readonly ttlInSeconds;
    private readonly getAsyncFromRedis;
    private readonly setexAsyncFromRedis;
    private readonly delAsyncFromRedis;
    constructor(client: RedisClient, ttlInSeconds?: number);
    read(stateKeys: string[]): Promise<StoreItems>;
    write(changes: StoreItems): Promise<void>;
    delete(keys: string[]): Promise<void>;
}
